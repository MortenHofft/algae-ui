import { create } from '@storybook/theming';
// import tokens from '../tokens';

export default create({
  base: 'light',

  // Branding information
  brandTitle: 'GBIF Algae UI',
  brandUrl: 'https://www.gbif.org'
});
